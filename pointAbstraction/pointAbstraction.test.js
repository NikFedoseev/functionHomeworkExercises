const {
  makePoint,
  getX,
  getY,
  toString,
  getDistance,
  getQuadrant
} = require("./pointAbstraction");

describe("pointAbstraction", () => {
  test("getX", () => {});
  test("getY", () => {});
  test("toString", () => {});
  test("getDistance", () => {});
  test("getQuadrant", () => {});
});
